<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="30dp"
    card_view:cardBackgroundColor="@color/cardview_light_background"
    card_view:cardCornerRadius="4dp"
    card_view:cardElevation="1dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <include
            android:id="@+id/post_buttons"
            layout="@layout/post_buttons" />

        <include
            android:id="@+id/post_header"
            layout="@layout/post_header" />

        <TextView
            android:id="@+id/post_photo_comment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginStart="8dp"
            android:layout_below="@id/post_header"
            android:textSize="18sp"
            android:text="Test media comment."/>

        <ImageView
            android:id="@+id/post_photo"
            android:layout_width="match_parent"
            android:layout_height="400dp"
            android:layout_marginTop="4dp"
            android:layout_below="@id/post_photo_comment"
            android:layout_above="@id/post_buttons"
            android:visibility="invisible"
            android:src="@mipmap/ic_launcher"/>

        <ProgressBar
            android:id="@+id/post_photo_progress"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginTop="4dp"
            android:layout_below="@id/post_photo_comment"
            android:layout_above="@id/post_buttons"
            android:layout_centerHorizontal="true"
            android:src="@mipmap/ic_launcher"/>

    </RelativeLayout>

</android.support.v7.widget.CardView>